// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;
import pl.wroc.pwr.wiz.io.psi.model.Powiadomienie;
import pl.wroc.pwr.wiz.io.psi.model.PowiadomienieDataOnDemand;

privileged aspect PowiadomienieDataOnDemand_Roo_DataOnDemand {
    
    declare @type: PowiadomienieDataOnDemand: @Component;
    
    private Random PowiadomienieDataOnDemand.rnd = new SecureRandom();
    
    private List<Powiadomienie> PowiadomienieDataOnDemand.data;
    
    public Powiadomienie PowiadomienieDataOnDemand.getNewTransientPowiadomienie(int index) {
        Powiadomienie obj = new Powiadomienie();
        setTresc(obj, index);
        return obj;
    }
    
    public void PowiadomienieDataOnDemand.setTresc(Powiadomienie obj, int index) {
        String tresc = "tresc_" + index;
        obj.setTresc(tresc);
    }
    
    public Powiadomienie PowiadomienieDataOnDemand.getSpecificPowiadomienie(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Powiadomienie obj = data.get(index);
        Long id = obj.getId();
        return Powiadomienie.findPowiadomienie(id);
    }
    
    public Powiadomienie PowiadomienieDataOnDemand.getRandomPowiadomienie() {
        init();
        Powiadomienie obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return Powiadomienie.findPowiadomienie(id);
    }
    
    public boolean PowiadomienieDataOnDemand.modifyPowiadomienie(Powiadomienie obj) {
        return false;
    }
    
    public void PowiadomienieDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Powiadomienie.findPowiadomienieEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Powiadomienie' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Powiadomienie>();
        for (int i = 0; i < 10; i++) {
            Powiadomienie obj = getNewTransientPowiadomienie(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
