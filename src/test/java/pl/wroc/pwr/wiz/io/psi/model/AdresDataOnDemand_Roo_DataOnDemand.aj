// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;
import pl.wroc.pwr.wiz.io.psi.model.Adres;
import pl.wroc.pwr.wiz.io.psi.model.AdresDataOnDemand;
import pl.wroc.pwr.wiz.io.psi.model.KrajDataOnDemand;

privileged aspect AdresDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AdresDataOnDemand: @Component;
    
    private Random AdresDataOnDemand.rnd = new SecureRandom();
    
    private List<Adres> AdresDataOnDemand.data;
    
    @Autowired
    KrajDataOnDemand AdresDataOnDemand.krajDataOnDemand;
    
    public Adres AdresDataOnDemand.getNewTransientAdres(int index) {
        Adres obj = new Adres();
        setKodPocztowy(obj, index);
        setMiejscowosc(obj, index);
        setUlicaZNumerem(obj, index);
        return obj;
    }
    
    public void AdresDataOnDemand.setKodPocztowy(Adres obj, int index) {
        String kodPocztowy = "kodPocztowy_" + index;
        obj.setKodPocztowy(kodPocztowy);
    }
    
    public void AdresDataOnDemand.setMiejscowosc(Adres obj, int index) {
        String miejscowosc = "miejscowosc_" + index;
        obj.setMiejscowosc(miejscowosc);
    }
    
    public void AdresDataOnDemand.setUlicaZNumerem(Adres obj, int index) {
        String ulicaZNumerem = "ulicaZNumerem_" + index;
        obj.setUlicaZNumerem(ulicaZNumerem);
    }
    
    public Adres AdresDataOnDemand.getSpecificAdres(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Adres obj = data.get(index);
        Long id = obj.getId();
        return Adres.findAdres(id);
    }
    
    public Adres AdresDataOnDemand.getRandomAdres() {
        init();
        Adres obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return Adres.findAdres(id);
    }
    
    public boolean AdresDataOnDemand.modifyAdres(Adres obj) {
        return false;
    }
    
    public void AdresDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Adres.findAdresEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Adres' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Adres>();
        for (int i = 0; i < 10; i++) {
            Adres obj = getNewTransientAdres(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
