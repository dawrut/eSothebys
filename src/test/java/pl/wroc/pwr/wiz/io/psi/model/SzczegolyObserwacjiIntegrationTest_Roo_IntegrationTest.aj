// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import pl.wroc.pwr.wiz.io.psi.model.SzczegolyObserwacji;
import pl.wroc.pwr.wiz.io.psi.model.SzczegolyObserwacjiDataOnDemand;
import pl.wroc.pwr.wiz.io.psi.model.SzczegolyObserwacjiIntegrationTest;

privileged aspect SzczegolyObserwacjiIntegrationTest_Roo_IntegrationTest {
    
    declare @type: SzczegolyObserwacjiIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: SzczegolyObserwacjiIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: SzczegolyObserwacjiIntegrationTest: @Transactional;
    
    @Autowired
    SzczegolyObserwacjiDataOnDemand SzczegolyObserwacjiIntegrationTest.dod;
    
    @Test
    public void SzczegolyObserwacjiIntegrationTest.testCountSzczegolyObserwacjis() {
        Assert.assertNotNull("Data on demand for 'SzczegolyObserwacji' failed to initialize correctly", dod.getRandomSzczegolyObserwacji());
        long count = SzczegolyObserwacji.countSzczegolyObserwacjis();
        Assert.assertTrue("Counter for 'SzczegolyObserwacji' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void SzczegolyObserwacjiIntegrationTest.testFindSzczegolyObserwacji() {
        SzczegolyObserwacji obj = dod.getRandomSzczegolyObserwacji();
        Assert.assertNotNull("Data on demand for 'SzczegolyObserwacji' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SzczegolyObserwacji' failed to provide an identifier", id);
        obj = SzczegolyObserwacji.findSzczegolyObserwacji(id);
        Assert.assertNotNull("Find method for 'SzczegolyObserwacji' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'SzczegolyObserwacji' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void SzczegolyObserwacjiIntegrationTest.testFindAllSzczegolyObserwacjis() {
        Assert.assertNotNull("Data on demand for 'SzczegolyObserwacji' failed to initialize correctly", dod.getRandomSzczegolyObserwacji());
        long count = SzczegolyObserwacji.countSzczegolyObserwacjis();
        Assert.assertTrue("Too expensive to perform a find all test for 'SzczegolyObserwacji', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<SzczegolyObserwacji> result = SzczegolyObserwacji.findAllSzczegolyObserwacjis();
        Assert.assertNotNull("Find all method for 'SzczegolyObserwacji' illegally returned null", result);
        Assert.assertTrue("Find all method for 'SzczegolyObserwacji' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void SzczegolyObserwacjiIntegrationTest.testFindSzczegolyObserwacjiEntries() {
        Assert.assertNotNull("Data on demand for 'SzczegolyObserwacji' failed to initialize correctly", dod.getRandomSzczegolyObserwacji());
        long count = SzczegolyObserwacji.countSzczegolyObserwacjis();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<SzczegolyObserwacji> result = SzczegolyObserwacji.findSzczegolyObserwacjiEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'SzczegolyObserwacji' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'SzczegolyObserwacji' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void SzczegolyObserwacjiIntegrationTest.testFlush() {
        SzczegolyObserwacji obj = dod.getRandomSzczegolyObserwacji();
        Assert.assertNotNull("Data on demand for 'SzczegolyObserwacji' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SzczegolyObserwacji' failed to provide an identifier", id);
        obj = SzczegolyObserwacji.findSzczegolyObserwacji(id);
        Assert.assertNotNull("Find method for 'SzczegolyObserwacji' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifySzczegolyObserwacji(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'SzczegolyObserwacji' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void SzczegolyObserwacjiIntegrationTest.testMergeUpdate() {
        SzczegolyObserwacji obj = dod.getRandomSzczegolyObserwacji();
        Assert.assertNotNull("Data on demand for 'SzczegolyObserwacji' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SzczegolyObserwacji' failed to provide an identifier", id);
        obj = SzczegolyObserwacji.findSzczegolyObserwacji(id);
        boolean modified =  dod.modifySzczegolyObserwacji(obj);
        Integer currentVersion = obj.getVersion();
        SzczegolyObserwacji merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'SzczegolyObserwacji' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void SzczegolyObserwacjiIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'SzczegolyObserwacji' failed to initialize correctly", dod.getRandomSzczegolyObserwacji());
        SzczegolyObserwacji obj = dod.getNewTransientSzczegolyObserwacji(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'SzczegolyObserwacji' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'SzczegolyObserwacji' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'SzczegolyObserwacji' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void SzczegolyObserwacjiIntegrationTest.testRemove() {
        SzczegolyObserwacji obj = dod.getRandomSzczegolyObserwacji();
        Assert.assertNotNull("Data on demand for 'SzczegolyObserwacji' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SzczegolyObserwacji' failed to provide an identifier", id);
        obj = SzczegolyObserwacji.findSzczegolyObserwacji(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'SzczegolyObserwacji' with identifier '" + id + "'", SzczegolyObserwacji.findSzczegolyObserwacji(id));
    }
    
}
