// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import pl.wroc.pwr.wiz.io.psi.model.UstawieniaSprzedazy;
import pl.wroc.pwr.wiz.io.psi.model.UstawieniaSprzedazyDataOnDemand;
import pl.wroc.pwr.wiz.io.psi.model.UstawieniaSprzedazyIntegrationTest;

privileged aspect UstawieniaSprzedazyIntegrationTest_Roo_IntegrationTest {
    
    declare @type: UstawieniaSprzedazyIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UstawieniaSprzedazyIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UstawieniaSprzedazyIntegrationTest: @Transactional;
    
    @Autowired
    UstawieniaSprzedazyDataOnDemand UstawieniaSprzedazyIntegrationTest.dod;
    
    @Test
    public void UstawieniaSprzedazyIntegrationTest.testCountUstawieniaSprzedazys() {
        Assert.assertNotNull("Data on demand for 'UstawieniaSprzedazy' failed to initialize correctly", dod.getRandomUstawieniaSprzedazy());
        long count = UstawieniaSprzedazy.countUstawieniaSprzedazys();
        Assert.assertTrue("Counter for 'UstawieniaSprzedazy' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UstawieniaSprzedazyIntegrationTest.testFindUstawieniaSprzedazy() {
        UstawieniaSprzedazy obj = dod.getRandomUstawieniaSprzedazy();
        Assert.assertNotNull("Data on demand for 'UstawieniaSprzedazy' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UstawieniaSprzedazy' failed to provide an identifier", id);
        obj = UstawieniaSprzedazy.findUstawieniaSprzedazy(id);
        Assert.assertNotNull("Find method for 'UstawieniaSprzedazy' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'UstawieniaSprzedazy' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void UstawieniaSprzedazyIntegrationTest.testFindAllUstawieniaSprzedazys() {
        Assert.assertNotNull("Data on demand for 'UstawieniaSprzedazy' failed to initialize correctly", dod.getRandomUstawieniaSprzedazy());
        long count = UstawieniaSprzedazy.countUstawieniaSprzedazys();
        Assert.assertTrue("Too expensive to perform a find all test for 'UstawieniaSprzedazy', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<UstawieniaSprzedazy> result = UstawieniaSprzedazy.findAllUstawieniaSprzedazys();
        Assert.assertNotNull("Find all method for 'UstawieniaSprzedazy' illegally returned null", result);
        Assert.assertTrue("Find all method for 'UstawieniaSprzedazy' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UstawieniaSprzedazyIntegrationTest.testFindUstawieniaSprzedazyEntries() {
        Assert.assertNotNull("Data on demand for 'UstawieniaSprzedazy' failed to initialize correctly", dod.getRandomUstawieniaSprzedazy());
        long count = UstawieniaSprzedazy.countUstawieniaSprzedazys();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<UstawieniaSprzedazy> result = UstawieniaSprzedazy.findUstawieniaSprzedazyEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'UstawieniaSprzedazy' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'UstawieniaSprzedazy' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UstawieniaSprzedazyIntegrationTest.testFlush() {
        UstawieniaSprzedazy obj = dod.getRandomUstawieniaSprzedazy();
        Assert.assertNotNull("Data on demand for 'UstawieniaSprzedazy' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UstawieniaSprzedazy' failed to provide an identifier", id);
        obj = UstawieniaSprzedazy.findUstawieniaSprzedazy(id);
        Assert.assertNotNull("Find method for 'UstawieniaSprzedazy' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyUstawieniaSprzedazy(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'UstawieniaSprzedazy' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void UstawieniaSprzedazyIntegrationTest.testMergeUpdate() {
        UstawieniaSprzedazy obj = dod.getRandomUstawieniaSprzedazy();
        Assert.assertNotNull("Data on demand for 'UstawieniaSprzedazy' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UstawieniaSprzedazy' failed to provide an identifier", id);
        obj = UstawieniaSprzedazy.findUstawieniaSprzedazy(id);
        boolean modified =  dod.modifyUstawieniaSprzedazy(obj);
        Integer currentVersion = obj.getVersion();
        UstawieniaSprzedazy merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'UstawieniaSprzedazy' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void UstawieniaSprzedazyIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'UstawieniaSprzedazy' failed to initialize correctly", dod.getRandomUstawieniaSprzedazy());
        UstawieniaSprzedazy obj = dod.getNewTransientUstawieniaSprzedazy(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'UstawieniaSprzedazy' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'UstawieniaSprzedazy' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'UstawieniaSprzedazy' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void UstawieniaSprzedazyIntegrationTest.testRemove() {
        UstawieniaSprzedazy obj = dod.getRandomUstawieniaSprzedazy();
        Assert.assertNotNull("Data on demand for 'UstawieniaSprzedazy' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'UstawieniaSprzedazy' failed to provide an identifier", id);
        obj = UstawieniaSprzedazy.findUstawieniaSprzedazy(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'UstawieniaSprzedazy' with identifier '" + id + "'", UstawieniaSprzedazy.findUstawieniaSprzedazy(id));
    }
    
}
