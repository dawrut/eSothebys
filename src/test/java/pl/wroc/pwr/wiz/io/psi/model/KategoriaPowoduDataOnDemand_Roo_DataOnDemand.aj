// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;
import pl.wroc.pwr.wiz.io.psi.model.KategoriaPowodu;
import pl.wroc.pwr.wiz.io.psi.model.KategoriaPowoduDataOnDemand;

privileged aspect KategoriaPowoduDataOnDemand_Roo_DataOnDemand {
    
    declare @type: KategoriaPowoduDataOnDemand: @Component;
    
    private Random KategoriaPowoduDataOnDemand.rnd = new SecureRandom();
    
    private List<KategoriaPowodu> KategoriaPowoduDataOnDemand.data;
    
    public KategoriaPowodu KategoriaPowoduDataOnDemand.getNewTransientKategoriaPowodu(int index) {
        KategoriaPowodu obj = new KategoriaPowodu();
        setNazwa(obj, index);
        return obj;
    }
    
    public void KategoriaPowoduDataOnDemand.setNazwa(KategoriaPowodu obj, int index) {
        String nazwa = "nazwa_" + index;
        obj.setNazwa(nazwa);
    }
    
    public KategoriaPowodu KategoriaPowoduDataOnDemand.getSpecificKategoriaPowodu(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        KategoriaPowodu obj = data.get(index);
        Long id = obj.getId();
        return KategoriaPowodu.findKategoriaPowodu(id);
    }
    
    public KategoriaPowodu KategoriaPowoduDataOnDemand.getRandomKategoriaPowodu() {
        init();
        KategoriaPowodu obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return KategoriaPowodu.findKategoriaPowodu(id);
    }
    
    public boolean KategoriaPowoduDataOnDemand.modifyKategoriaPowodu(KategoriaPowodu obj) {
        return false;
    }
    
    public void KategoriaPowoduDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = KategoriaPowodu.findKategoriaPowoduEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'KategoriaPowodu' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<KategoriaPowodu>();
        for (int i = 0; i < 10; i++) {
            KategoriaPowodu obj = getNewTransientKategoriaPowodu(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
