// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;
import pl.wroc.pwr.wiz.io.psi.model.Uzytkownik;
import pl.wroc.pwr.wiz.io.psi.model.UzytkownikDataOnDemand;

privileged aspect UzytkownikDataOnDemand_Roo_DataOnDemand {
    
    declare @type: UzytkownikDataOnDemand: @Component;
    
    private Random UzytkownikDataOnDemand.rnd = new SecureRandom();
    
    private List<Uzytkownik> UzytkownikDataOnDemand.data;
    
    public Uzytkownik UzytkownikDataOnDemand.getNewTransientUzytkownik(int index) {
        Uzytkownik obj = new Uzytkownik();
        setEmail(obj, index);
        setHaslo(obj, index);
        return obj;
    }
    
    public void UzytkownikDataOnDemand.setEmail(Uzytkownik obj, int index) {
        String email = "foo" + index + "@bar.com";
        obj.setEmail(email);
    }
    
    public void UzytkownikDataOnDemand.setHaslo(Uzytkownik obj, int index) {
        String haslo = "haslo_" + index;
        obj.setHaslo(haslo);
    }
    
    public Uzytkownik UzytkownikDataOnDemand.getSpecificUzytkownik(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Uzytkownik obj = data.get(index);
        Long id = obj.getId();
        return Uzytkownik.findUzytkownik(id);
    }
    
    public Uzytkownik UzytkownikDataOnDemand.getRandomUzytkownik() {
        init();
        Uzytkownik obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return Uzytkownik.findUzytkownik(id);
    }
    
    public boolean UzytkownikDataOnDemand.modifyUzytkownik(Uzytkownik obj) {
        return false;
    }
    
    public void UzytkownikDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Uzytkownik.findUzytkownikEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Uzytkownik' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Uzytkownik>();
        for (int i = 0; i < 10; i++) {
            Uzytkownik obj = getNewTransientUzytkownik(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
