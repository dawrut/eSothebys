// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;
import pl.wroc.pwr.wiz.io.psi.model.ZawieszenieKonta;
import pl.wroc.pwr.wiz.io.psi.model.ZawieszenieKontaDataOnDemand;

privileged aspect ZawieszenieKontaDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ZawieszenieKontaDataOnDemand: @Component;
    
    private Random ZawieszenieKontaDataOnDemand.rnd = new SecureRandom();
    
    private List<ZawieszenieKonta> ZawieszenieKontaDataOnDemand.data;
    
    public ZawieszenieKonta ZawieszenieKontaDataOnDemand.getNewTransientZawieszenieKonta(int index) {
        ZawieszenieKonta obj = new ZawieszenieKonta();
        setCzyPerm(obj, index);
        setDataDo(obj, index);
        setDataOd(obj, index);
        setPowod(obj, index);
        return obj;
    }
    
    public void ZawieszenieKontaDataOnDemand.setCzyPerm(ZawieszenieKonta obj, int index) {
        Boolean czyPerm = Boolean.TRUE;
        obj.setCzyPerm(czyPerm);
    }
    
    public void ZawieszenieKontaDataOnDemand.setDataDo(ZawieszenieKonta obj, int index) {
        Date dataDo = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDataDo(dataDo);
    }
    
    public void ZawieszenieKontaDataOnDemand.setDataOd(ZawieszenieKonta obj, int index) {
        Date dataOd = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDataOd(dataOd);
    }
    
    public void ZawieszenieKontaDataOnDemand.setPowod(ZawieszenieKonta obj, int index) {
        String powod = "powod_" + index;
        obj.setPowod(powod);
    }
    
    public ZawieszenieKonta ZawieszenieKontaDataOnDemand.getSpecificZawieszenieKonta(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ZawieszenieKonta obj = data.get(index);
        Long id = obj.getId();
        return ZawieszenieKonta.findZawieszenieKonta(id);
    }
    
    public ZawieszenieKonta ZawieszenieKontaDataOnDemand.getRandomZawieszenieKonta() {
        init();
        ZawieszenieKonta obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return ZawieszenieKonta.findZawieszenieKonta(id);
    }
    
    public boolean ZawieszenieKontaDataOnDemand.modifyZawieszenieKonta(ZawieszenieKonta obj) {
        return false;
    }
    
    public void ZawieszenieKontaDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ZawieszenieKonta.findZawieszenieKontaEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ZawieszenieKonta' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ZawieszenieKonta>();
        for (int i = 0; i < 10; i++) {
            ZawieszenieKonta obj = getNewTransientZawieszenieKonta(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
