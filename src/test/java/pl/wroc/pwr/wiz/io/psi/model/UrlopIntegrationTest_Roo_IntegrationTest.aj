// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import pl.wroc.pwr.wiz.io.psi.model.Urlop;
import pl.wroc.pwr.wiz.io.psi.model.UrlopDataOnDemand;
import pl.wroc.pwr.wiz.io.psi.model.UrlopIntegrationTest;

privileged aspect UrlopIntegrationTest_Roo_IntegrationTest {
    
    declare @type: UrlopIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UrlopIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UrlopIntegrationTest: @Transactional;
    
    @Autowired
    UrlopDataOnDemand UrlopIntegrationTest.dod;
    
    @Test
    public void UrlopIntegrationTest.testCountUrlops() {
        Assert.assertNotNull("Data on demand for 'Urlop' failed to initialize correctly", dod.getRandomUrlop());
        long count = Urlop.countUrlops();
        Assert.assertTrue("Counter for 'Urlop' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UrlopIntegrationTest.testFindUrlop() {
        Urlop obj = dod.getRandomUrlop();
        Assert.assertNotNull("Data on demand for 'Urlop' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Urlop' failed to provide an identifier", id);
        obj = Urlop.findUrlop(id);
        Assert.assertNotNull("Find method for 'Urlop' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Urlop' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void UrlopIntegrationTest.testFindAllUrlops() {
        Assert.assertNotNull("Data on demand for 'Urlop' failed to initialize correctly", dod.getRandomUrlop());
        long count = Urlop.countUrlops();
        Assert.assertTrue("Too expensive to perform a find all test for 'Urlop', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Urlop> result = Urlop.findAllUrlops();
        Assert.assertNotNull("Find all method for 'Urlop' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Urlop' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UrlopIntegrationTest.testFindUrlopEntries() {
        Assert.assertNotNull("Data on demand for 'Urlop' failed to initialize correctly", dod.getRandomUrlop());
        long count = Urlop.countUrlops();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Urlop> result = Urlop.findUrlopEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Urlop' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Urlop' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UrlopIntegrationTest.testFlush() {
        Urlop obj = dod.getRandomUrlop();
        Assert.assertNotNull("Data on demand for 'Urlop' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Urlop' failed to provide an identifier", id);
        obj = Urlop.findUrlop(id);
        Assert.assertNotNull("Find method for 'Urlop' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyUrlop(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Urlop' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void UrlopIntegrationTest.testMergeUpdate() {
        Urlop obj = dod.getRandomUrlop();
        Assert.assertNotNull("Data on demand for 'Urlop' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Urlop' failed to provide an identifier", id);
        obj = Urlop.findUrlop(id);
        boolean modified =  dod.modifyUrlop(obj);
        Integer currentVersion = obj.getVersion();
        Urlop merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'Urlop' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void UrlopIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Urlop' failed to initialize correctly", dod.getRandomUrlop());
        Urlop obj = dod.getNewTransientUrlop(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Urlop' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Urlop' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'Urlop' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void UrlopIntegrationTest.testRemove() {
        Urlop obj = dod.getRandomUrlop();
        Assert.assertNotNull("Data on demand for 'Urlop' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Urlop' failed to provide an identifier", id);
        obj = Urlop.findUrlop(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Urlop' with identifier '" + id + "'", Urlop.findUrlop(id));
    }
    
}
