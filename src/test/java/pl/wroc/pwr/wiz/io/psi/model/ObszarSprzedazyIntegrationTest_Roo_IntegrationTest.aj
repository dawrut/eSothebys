// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import pl.wroc.pwr.wiz.io.psi.model.ObszarSprzedazy;
import pl.wroc.pwr.wiz.io.psi.model.ObszarSprzedazyDataOnDemand;
import pl.wroc.pwr.wiz.io.psi.model.ObszarSprzedazyIntegrationTest;

privileged aspect ObszarSprzedazyIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ObszarSprzedazyIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ObszarSprzedazyIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ObszarSprzedazyIntegrationTest: @Transactional;
    
    @Autowired
    ObszarSprzedazyDataOnDemand ObszarSprzedazyIntegrationTest.dod;
    
    @Test
    public void ObszarSprzedazyIntegrationTest.testCountObszarSprzedazys() {
        Assert.assertNotNull("Data on demand for 'ObszarSprzedazy' failed to initialize correctly", dod.getRandomObszarSprzedazy());
        long count = ObszarSprzedazy.countObszarSprzedazys();
        Assert.assertTrue("Counter for 'ObszarSprzedazy' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ObszarSprzedazyIntegrationTest.testFindObszarSprzedazy() {
        ObszarSprzedazy obj = dod.getRandomObszarSprzedazy();
        Assert.assertNotNull("Data on demand for 'ObszarSprzedazy' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ObszarSprzedazy' failed to provide an identifier", id);
        obj = ObszarSprzedazy.findObszarSprzedazy(id);
        Assert.assertNotNull("Find method for 'ObszarSprzedazy' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ObszarSprzedazy' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ObszarSprzedazyIntegrationTest.testFindAllObszarSprzedazys() {
        Assert.assertNotNull("Data on demand for 'ObszarSprzedazy' failed to initialize correctly", dod.getRandomObszarSprzedazy());
        long count = ObszarSprzedazy.countObszarSprzedazys();
        Assert.assertTrue("Too expensive to perform a find all test for 'ObszarSprzedazy', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ObszarSprzedazy> result = ObszarSprzedazy.findAllObszarSprzedazys();
        Assert.assertNotNull("Find all method for 'ObszarSprzedazy' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ObszarSprzedazy' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ObszarSprzedazyIntegrationTest.testFindObszarSprzedazyEntries() {
        Assert.assertNotNull("Data on demand for 'ObszarSprzedazy' failed to initialize correctly", dod.getRandomObszarSprzedazy());
        long count = ObszarSprzedazy.countObszarSprzedazys();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ObszarSprzedazy> result = ObszarSprzedazy.findObszarSprzedazyEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ObszarSprzedazy' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ObszarSprzedazy' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ObszarSprzedazyIntegrationTest.testFlush() {
        ObszarSprzedazy obj = dod.getRandomObszarSprzedazy();
        Assert.assertNotNull("Data on demand for 'ObszarSprzedazy' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ObszarSprzedazy' failed to provide an identifier", id);
        obj = ObszarSprzedazy.findObszarSprzedazy(id);
        Assert.assertNotNull("Find method for 'ObszarSprzedazy' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyObszarSprzedazy(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ObszarSprzedazy' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ObszarSprzedazyIntegrationTest.testMergeUpdate() {
        ObszarSprzedazy obj = dod.getRandomObszarSprzedazy();
        Assert.assertNotNull("Data on demand for 'ObszarSprzedazy' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ObszarSprzedazy' failed to provide an identifier", id);
        obj = ObszarSprzedazy.findObszarSprzedazy(id);
        boolean modified =  dod.modifyObszarSprzedazy(obj);
        Integer currentVersion = obj.getVersion();
        ObszarSprzedazy merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ObszarSprzedazy' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ObszarSprzedazyIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ObszarSprzedazy' failed to initialize correctly", dod.getRandomObszarSprzedazy());
        ObszarSprzedazy obj = dod.getNewTransientObszarSprzedazy(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ObszarSprzedazy' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ObszarSprzedazy' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'ObszarSprzedazy' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ObszarSprzedazyIntegrationTest.testRemove() {
        ObszarSprzedazy obj = dod.getRandomObszarSprzedazy();
        Assert.assertNotNull("Data on demand for 'ObszarSprzedazy' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ObszarSprzedazy' failed to provide an identifier", id);
        obj = ObszarSprzedazy.findObszarSprzedazy(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ObszarSprzedazy' with identifier '" + id + "'", ObszarSprzedazy.findObszarSprzedazy(id));
    }
    
}
