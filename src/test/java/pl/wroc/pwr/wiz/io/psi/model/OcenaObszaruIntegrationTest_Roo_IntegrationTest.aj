// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import pl.wroc.pwr.wiz.io.psi.model.OcenaObszaru;
import pl.wroc.pwr.wiz.io.psi.model.OcenaObszaruDataOnDemand;
import pl.wroc.pwr.wiz.io.psi.model.OcenaObszaruIntegrationTest;

privileged aspect OcenaObszaruIntegrationTest_Roo_IntegrationTest {
    
    declare @type: OcenaObszaruIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: OcenaObszaruIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: OcenaObszaruIntegrationTest: @Transactional;
    
    @Autowired
    OcenaObszaruDataOnDemand OcenaObszaruIntegrationTest.dod;
    
    @Test
    public void OcenaObszaruIntegrationTest.testCountOcenaObszarus() {
        Assert.assertNotNull("Data on demand for 'OcenaObszaru' failed to initialize correctly", dod.getRandomOcenaObszaru());
        long count = OcenaObszaru.countOcenaObszarus();
        Assert.assertTrue("Counter for 'OcenaObszaru' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void OcenaObszaruIntegrationTest.testFindOcenaObszaru() {
        OcenaObszaru obj = dod.getRandomOcenaObszaru();
        Assert.assertNotNull("Data on demand for 'OcenaObszaru' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'OcenaObszaru' failed to provide an identifier", id);
        obj = OcenaObszaru.findOcenaObszaru(id);
        Assert.assertNotNull("Find method for 'OcenaObszaru' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'OcenaObszaru' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void OcenaObszaruIntegrationTest.testFindAllOcenaObszarus() {
        Assert.assertNotNull("Data on demand for 'OcenaObszaru' failed to initialize correctly", dod.getRandomOcenaObszaru());
        long count = OcenaObszaru.countOcenaObszarus();
        Assert.assertTrue("Too expensive to perform a find all test for 'OcenaObszaru', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<OcenaObszaru> result = OcenaObszaru.findAllOcenaObszarus();
        Assert.assertNotNull("Find all method for 'OcenaObszaru' illegally returned null", result);
        Assert.assertTrue("Find all method for 'OcenaObszaru' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void OcenaObszaruIntegrationTest.testFindOcenaObszaruEntries() {
        Assert.assertNotNull("Data on demand for 'OcenaObszaru' failed to initialize correctly", dod.getRandomOcenaObszaru());
        long count = OcenaObszaru.countOcenaObszarus();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<OcenaObszaru> result = OcenaObszaru.findOcenaObszaruEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'OcenaObszaru' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'OcenaObszaru' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void OcenaObszaruIntegrationTest.testFlush() {
        OcenaObszaru obj = dod.getRandomOcenaObszaru();
        Assert.assertNotNull("Data on demand for 'OcenaObszaru' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'OcenaObszaru' failed to provide an identifier", id);
        obj = OcenaObszaru.findOcenaObszaru(id);
        Assert.assertNotNull("Find method for 'OcenaObszaru' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyOcenaObszaru(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'OcenaObszaru' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void OcenaObszaruIntegrationTest.testMergeUpdate() {
        OcenaObszaru obj = dod.getRandomOcenaObszaru();
        Assert.assertNotNull("Data on demand for 'OcenaObszaru' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'OcenaObszaru' failed to provide an identifier", id);
        obj = OcenaObszaru.findOcenaObszaru(id);
        boolean modified =  dod.modifyOcenaObszaru(obj);
        Integer currentVersion = obj.getVersion();
        OcenaObszaru merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'OcenaObszaru' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void OcenaObszaruIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'OcenaObszaru' failed to initialize correctly", dod.getRandomOcenaObszaru());
        OcenaObszaru obj = dod.getNewTransientOcenaObszaru(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'OcenaObszaru' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'OcenaObszaru' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'OcenaObszaru' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void OcenaObszaruIntegrationTest.testRemove() {
        OcenaObszaru obj = dod.getRandomOcenaObszaru();
        Assert.assertNotNull("Data on demand for 'OcenaObszaru' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'OcenaObszaru' failed to provide an identifier", id);
        obj = OcenaObszaru.findOcenaObszaru(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'OcenaObszaru' with identifier '" + id + "'", OcenaObszaru.findOcenaObszaru(id));
    }
    
}
