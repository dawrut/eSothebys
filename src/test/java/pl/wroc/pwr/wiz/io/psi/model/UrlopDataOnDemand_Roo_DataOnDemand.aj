// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;
import pl.wroc.pwr.wiz.io.psi.model.Urlop;
import pl.wroc.pwr.wiz.io.psi.model.UrlopDataOnDemand;

privileged aspect UrlopDataOnDemand_Roo_DataOnDemand {
    
    declare @type: UrlopDataOnDemand: @Component;
    
    private Random UrlopDataOnDemand.rnd = new SecureRandom();
    
    private List<Urlop> UrlopDataOnDemand.data;
    
    public Urlop UrlopDataOnDemand.getNewTransientUrlop(int index) {
        Urlop obj = new Urlop();
        setDataRozpoczecia(obj, index);
        setDataZakonczenia(obj, index);
        return obj;
    }
    
    public void UrlopDataOnDemand.setDataRozpoczecia(Urlop obj, int index) {
        Date dataRozpoczecia = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDataRozpoczecia(dataRozpoczecia);
    }
    
    public void UrlopDataOnDemand.setDataZakonczenia(Urlop obj, int index) {
        Date dataZakonczenia = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setDataZakonczenia(dataZakonczenia);
    }
    
    public Urlop UrlopDataOnDemand.getSpecificUrlop(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Urlop obj = data.get(index);
        Long id = obj.getId();
        return Urlop.findUrlop(id);
    }
    
    public Urlop UrlopDataOnDemand.getRandomUrlop() {
        init();
        Urlop obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return Urlop.findUrlop(id);
    }
    
    public boolean UrlopDataOnDemand.modifyUrlop(Urlop obj) {
        return false;
    }
    
    public void UrlopDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Urlop.findUrlopEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Urlop' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Urlop>();
        for (int i = 0; i < 10; i++) {
            Urlop obj = getNewTransientUrlop(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
