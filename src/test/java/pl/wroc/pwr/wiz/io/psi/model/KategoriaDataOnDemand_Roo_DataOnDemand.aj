// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;
import pl.wroc.pwr.wiz.io.psi.model.Kategoria;
import pl.wroc.pwr.wiz.io.psi.model.KategoriaDataOnDemand;

privileged aspect KategoriaDataOnDemand_Roo_DataOnDemand {
    
    declare @type: KategoriaDataOnDemand: @Component;
    
    private Random KategoriaDataOnDemand.rnd = new SecureRandom();
    
    private List<Kategoria> KategoriaDataOnDemand.data;
    
    public Kategoria KategoriaDataOnDemand.getNewTransientKategoria(int index) {
        Kategoria obj = new Kategoria();
        setAukcjeJednoprzedmiotowe(obj, index);
        setDostepnaPlatoscKarta(obj, index);
        setDostepnaPlatoscPrzezPayU(obj, index);
        setNazwa(obj, index);
        return obj;
    }
    
    public void KategoriaDataOnDemand.setAukcjeJednoprzedmiotowe(Kategoria obj, int index) {
        Boolean aukcjeJednoprzedmiotowe = Boolean.TRUE;
        obj.setAukcjeJednoprzedmiotowe(aukcjeJednoprzedmiotowe);
    }
    
    public void KategoriaDataOnDemand.setDostepnaPlatoscKarta(Kategoria obj, int index) {
        Boolean dostepnaPlatoscKarta = Boolean.TRUE;
        obj.setDostepnaPlatoscKarta(dostepnaPlatoscKarta);
    }
    
    public void KategoriaDataOnDemand.setDostepnaPlatoscPrzezPayU(Kategoria obj, int index) {
        Boolean dostepnaPlatoscPrzezPayU = Boolean.TRUE;
        obj.setDostepnaPlatoscPrzezPayU(dostepnaPlatoscPrzezPayU);
    }
    
    public void KategoriaDataOnDemand.setNazwa(Kategoria obj, int index) {
        String nazwa = "nazwa_" + index;
        obj.setNazwa(nazwa);
    }
    
    public Kategoria KategoriaDataOnDemand.getSpecificKategoria(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Kategoria obj = data.get(index);
        Long id = obj.getId();
        return Kategoria.findKategoria(id);
    }
    
    public Kategoria KategoriaDataOnDemand.getRandomKategoria() {
        init();
        Kategoria obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return Kategoria.findKategoria(id);
    }
    
    public boolean KategoriaDataOnDemand.modifyKategoria(Kategoria obj) {
        return false;
    }
    
    public void KategoriaDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = Kategoria.findKategoriaEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Kategoria' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Kategoria>();
        for (int i = 0; i < 10; i++) {
            Kategoria obj = getNewTransientKategoria(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
