// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import pl.wroc.pwr.wiz.io.psi.model.PowodZgloszenia;
import pl.wroc.pwr.wiz.io.psi.model.PowodZgloszeniaDataOnDemand;
import pl.wroc.pwr.wiz.io.psi.model.PowodZgloszeniaIntegrationTest;

privileged aspect PowodZgloszeniaIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PowodZgloszeniaIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PowodZgloszeniaIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PowodZgloszeniaIntegrationTest: @Transactional;
    
    @Autowired
    PowodZgloszeniaDataOnDemand PowodZgloszeniaIntegrationTest.dod;
    
    @Test
    public void PowodZgloszeniaIntegrationTest.testCountPowodZgloszenias() {
        Assert.assertNotNull("Data on demand for 'PowodZgloszenia' failed to initialize correctly", dod.getRandomPowodZgloszenia());
        long count = PowodZgloszenia.countPowodZgloszenias();
        Assert.assertTrue("Counter for 'PowodZgloszenia' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PowodZgloszeniaIntegrationTest.testFindPowodZgloszenia() {
        PowodZgloszenia obj = dod.getRandomPowodZgloszenia();
        Assert.assertNotNull("Data on demand for 'PowodZgloszenia' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PowodZgloszenia' failed to provide an identifier", id);
        obj = PowodZgloszenia.findPowodZgloszenia(id);
        Assert.assertNotNull("Find method for 'PowodZgloszenia' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'PowodZgloszenia' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PowodZgloszeniaIntegrationTest.testFindAllPowodZgloszenias() {
        Assert.assertNotNull("Data on demand for 'PowodZgloszenia' failed to initialize correctly", dod.getRandomPowodZgloszenia());
        long count = PowodZgloszenia.countPowodZgloszenias();
        Assert.assertTrue("Too expensive to perform a find all test for 'PowodZgloszenia', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<PowodZgloszenia> result = PowodZgloszenia.findAllPowodZgloszenias();
        Assert.assertNotNull("Find all method for 'PowodZgloszenia' illegally returned null", result);
        Assert.assertTrue("Find all method for 'PowodZgloszenia' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PowodZgloszeniaIntegrationTest.testFindPowodZgloszeniaEntries() {
        Assert.assertNotNull("Data on demand for 'PowodZgloszenia' failed to initialize correctly", dod.getRandomPowodZgloszenia());
        long count = PowodZgloszenia.countPowodZgloszenias();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<PowodZgloszenia> result = PowodZgloszenia.findPowodZgloszeniaEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'PowodZgloszenia' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'PowodZgloszenia' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PowodZgloszeniaIntegrationTest.testFlush() {
        PowodZgloszenia obj = dod.getRandomPowodZgloszenia();
        Assert.assertNotNull("Data on demand for 'PowodZgloszenia' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PowodZgloszenia' failed to provide an identifier", id);
        obj = PowodZgloszenia.findPowodZgloszenia(id);
        Assert.assertNotNull("Find method for 'PowodZgloszenia' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyPowodZgloszenia(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'PowodZgloszenia' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void PowodZgloszeniaIntegrationTest.testMergeUpdate() {
        PowodZgloszenia obj = dod.getRandomPowodZgloszenia();
        Assert.assertNotNull("Data on demand for 'PowodZgloszenia' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PowodZgloszenia' failed to provide an identifier", id);
        obj = PowodZgloszenia.findPowodZgloszenia(id);
        boolean modified =  dod.modifyPowodZgloszenia(obj);
        Integer currentVersion = obj.getVersion();
        PowodZgloszenia merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'PowodZgloszenia' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void PowodZgloszeniaIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'PowodZgloszenia' failed to initialize correctly", dod.getRandomPowodZgloszenia());
        PowodZgloszenia obj = dod.getNewTransientPowodZgloszenia(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'PowodZgloszenia' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'PowodZgloszenia' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'PowodZgloszenia' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PowodZgloszeniaIntegrationTest.testRemove() {
        PowodZgloszenia obj = dod.getRandomPowodZgloszenia();
        Assert.assertNotNull("Data on demand for 'PowodZgloszenia' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PowodZgloszenia' failed to provide an identifier", id);
        obj = PowodZgloszenia.findPowodZgloszenia(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'PowodZgloszenia' with identifier '" + id + "'", PowodZgloszenia.findPowodZgloszenia(id));
    }
    
}
