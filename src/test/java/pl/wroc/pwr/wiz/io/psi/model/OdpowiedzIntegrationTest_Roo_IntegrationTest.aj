// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import pl.wroc.pwr.wiz.io.psi.model.Odpowiedz;
import pl.wroc.pwr.wiz.io.psi.model.OdpowiedzDataOnDemand;
import pl.wroc.pwr.wiz.io.psi.model.OdpowiedzIntegrationTest;

privileged aspect OdpowiedzIntegrationTest_Roo_IntegrationTest {
    
    declare @type: OdpowiedzIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: OdpowiedzIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: OdpowiedzIntegrationTest: @Transactional;
    
    @Autowired
    OdpowiedzDataOnDemand OdpowiedzIntegrationTest.dod;
    
    @Test
    public void OdpowiedzIntegrationTest.testCountOdpowiedzes() {
        Assert.assertNotNull("Data on demand for 'Odpowiedz' failed to initialize correctly", dod.getRandomOdpowiedz());
        long count = Odpowiedz.countOdpowiedzes();
        Assert.assertTrue("Counter for 'Odpowiedz' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void OdpowiedzIntegrationTest.testFindOdpowiedz() {
        Odpowiedz obj = dod.getRandomOdpowiedz();
        Assert.assertNotNull("Data on demand for 'Odpowiedz' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Odpowiedz' failed to provide an identifier", id);
        obj = Odpowiedz.findOdpowiedz(id);
        Assert.assertNotNull("Find method for 'Odpowiedz' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Odpowiedz' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void OdpowiedzIntegrationTest.testFindAllOdpowiedzes() {
        Assert.assertNotNull("Data on demand for 'Odpowiedz' failed to initialize correctly", dod.getRandomOdpowiedz());
        long count = Odpowiedz.countOdpowiedzes();
        Assert.assertTrue("Too expensive to perform a find all test for 'Odpowiedz', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Odpowiedz> result = Odpowiedz.findAllOdpowiedzes();
        Assert.assertNotNull("Find all method for 'Odpowiedz' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Odpowiedz' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void OdpowiedzIntegrationTest.testFindOdpowiedzEntries() {
        Assert.assertNotNull("Data on demand for 'Odpowiedz' failed to initialize correctly", dod.getRandomOdpowiedz());
        long count = Odpowiedz.countOdpowiedzes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Odpowiedz> result = Odpowiedz.findOdpowiedzEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Odpowiedz' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Odpowiedz' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void OdpowiedzIntegrationTest.testFlush() {
        Odpowiedz obj = dod.getRandomOdpowiedz();
        Assert.assertNotNull("Data on demand for 'Odpowiedz' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Odpowiedz' failed to provide an identifier", id);
        obj = Odpowiedz.findOdpowiedz(id);
        Assert.assertNotNull("Find method for 'Odpowiedz' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyOdpowiedz(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Odpowiedz' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void OdpowiedzIntegrationTest.testMergeUpdate() {
        Odpowiedz obj = dod.getRandomOdpowiedz();
        Assert.assertNotNull("Data on demand for 'Odpowiedz' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Odpowiedz' failed to provide an identifier", id);
        obj = Odpowiedz.findOdpowiedz(id);
        boolean modified =  dod.modifyOdpowiedz(obj);
        Integer currentVersion = obj.getVersion();
        Odpowiedz merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'Odpowiedz' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void OdpowiedzIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Odpowiedz' failed to initialize correctly", dod.getRandomOdpowiedz());
        Odpowiedz obj = dod.getNewTransientOdpowiedz(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Odpowiedz' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Odpowiedz' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'Odpowiedz' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void OdpowiedzIntegrationTest.testRemove() {
        Odpowiedz obj = dod.getRandomOdpowiedz();
        Assert.assertNotNull("Data on demand for 'Odpowiedz' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Odpowiedz' failed to provide an identifier", id);
        obj = Odpowiedz.findOdpowiedz(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Odpowiedz' with identifier '" + id + "'", Odpowiedz.findOdpowiedz(id));
    }
    
}
