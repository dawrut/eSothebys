// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;
import pl.wroc.pwr.wiz.io.psi.model.SubskrypcjaUlubionejKategorii;
import pl.wroc.pwr.wiz.io.psi.model.SubskrypcjaUlubionejKategoriiDataOnDemand;

privileged aspect SubskrypcjaUlubionejKategoriiDataOnDemand_Roo_DataOnDemand {
    
    declare @type: SubskrypcjaUlubionejKategoriiDataOnDemand: @Component;
    
    private Random SubskrypcjaUlubionejKategoriiDataOnDemand.rnd = new SecureRandom();
    
    private List<SubskrypcjaUlubionejKategorii> SubskrypcjaUlubionejKategoriiDataOnDemand.data;
    
    public SubskrypcjaUlubionejKategorii SubskrypcjaUlubionejKategoriiDataOnDemand.getNewTransientSubskrypcjaUlubionejKategorii(int index) {
        SubskrypcjaUlubionejKategorii obj = new SubskrypcjaUlubionejKategorii();
        setTylkoKupTeraz(obj, index);
        return obj;
    }
    
    public void SubskrypcjaUlubionejKategoriiDataOnDemand.setTylkoKupTeraz(SubskrypcjaUlubionejKategorii obj, int index) {
        Boolean tylkoKupTeraz = Boolean.TRUE;
        obj.setTylkoKupTeraz(tylkoKupTeraz);
    }
    
    public SubskrypcjaUlubionejKategorii SubskrypcjaUlubionejKategoriiDataOnDemand.getSpecificSubskrypcjaUlubionejKategorii(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        SubskrypcjaUlubionejKategorii obj = data.get(index);
        Long id = obj.getId();
        return SubskrypcjaUlubionejKategorii.findSubskrypcjaUlubionejKategorii(id);
    }
    
    public SubskrypcjaUlubionejKategorii SubskrypcjaUlubionejKategoriiDataOnDemand.getRandomSubskrypcjaUlubionejKategorii() {
        init();
        SubskrypcjaUlubionejKategorii obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return SubskrypcjaUlubionejKategorii.findSubskrypcjaUlubionejKategorii(id);
    }
    
    public boolean SubskrypcjaUlubionejKategoriiDataOnDemand.modifySubskrypcjaUlubionejKategorii(SubskrypcjaUlubionejKategorii obj) {
        return false;
    }
    
    public void SubskrypcjaUlubionejKategoriiDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = SubskrypcjaUlubionejKategorii.findSubskrypcjaUlubionejKategoriiEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'SubskrypcjaUlubionejKategorii' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<SubskrypcjaUlubionejKategorii>();
        for (int i = 0; i < 10; i++) {
            SubskrypcjaUlubionejKategorii obj = getNewTransientSubskrypcjaUlubionejKategorii(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
