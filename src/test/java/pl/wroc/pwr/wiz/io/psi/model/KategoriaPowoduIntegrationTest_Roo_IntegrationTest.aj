// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import pl.wroc.pwr.wiz.io.psi.model.KategoriaPowodu;
import pl.wroc.pwr.wiz.io.psi.model.KategoriaPowoduDataOnDemand;
import pl.wroc.pwr.wiz.io.psi.model.KategoriaPowoduIntegrationTest;

privileged aspect KategoriaPowoduIntegrationTest_Roo_IntegrationTest {
    
    declare @type: KategoriaPowoduIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: KategoriaPowoduIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: KategoriaPowoduIntegrationTest: @Transactional;
    
    @Autowired
    KategoriaPowoduDataOnDemand KategoriaPowoduIntegrationTest.dod;
    
    @Test
    public void KategoriaPowoduIntegrationTest.testCountKategoriaPowodus() {
        Assert.assertNotNull("Data on demand for 'KategoriaPowodu' failed to initialize correctly", dod.getRandomKategoriaPowodu());
        long count = KategoriaPowodu.countKategoriaPowodus();
        Assert.assertTrue("Counter for 'KategoriaPowodu' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void KategoriaPowoduIntegrationTest.testFindKategoriaPowodu() {
        KategoriaPowodu obj = dod.getRandomKategoriaPowodu();
        Assert.assertNotNull("Data on demand for 'KategoriaPowodu' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'KategoriaPowodu' failed to provide an identifier", id);
        obj = KategoriaPowodu.findKategoriaPowodu(id);
        Assert.assertNotNull("Find method for 'KategoriaPowodu' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'KategoriaPowodu' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void KategoriaPowoduIntegrationTest.testFindAllKategoriaPowodus() {
        Assert.assertNotNull("Data on demand for 'KategoriaPowodu' failed to initialize correctly", dod.getRandomKategoriaPowodu());
        long count = KategoriaPowodu.countKategoriaPowodus();
        Assert.assertTrue("Too expensive to perform a find all test for 'KategoriaPowodu', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<KategoriaPowodu> result = KategoriaPowodu.findAllKategoriaPowodus();
        Assert.assertNotNull("Find all method for 'KategoriaPowodu' illegally returned null", result);
        Assert.assertTrue("Find all method for 'KategoriaPowodu' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void KategoriaPowoduIntegrationTest.testFindKategoriaPowoduEntries() {
        Assert.assertNotNull("Data on demand for 'KategoriaPowodu' failed to initialize correctly", dod.getRandomKategoriaPowodu());
        long count = KategoriaPowodu.countKategoriaPowodus();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<KategoriaPowodu> result = KategoriaPowodu.findKategoriaPowoduEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'KategoriaPowodu' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'KategoriaPowodu' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void KategoriaPowoduIntegrationTest.testFlush() {
        KategoriaPowodu obj = dod.getRandomKategoriaPowodu();
        Assert.assertNotNull("Data on demand for 'KategoriaPowodu' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'KategoriaPowodu' failed to provide an identifier", id);
        obj = KategoriaPowodu.findKategoriaPowodu(id);
        Assert.assertNotNull("Find method for 'KategoriaPowodu' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyKategoriaPowodu(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'KategoriaPowodu' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void KategoriaPowoduIntegrationTest.testMergeUpdate() {
        KategoriaPowodu obj = dod.getRandomKategoriaPowodu();
        Assert.assertNotNull("Data on demand for 'KategoriaPowodu' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'KategoriaPowodu' failed to provide an identifier", id);
        obj = KategoriaPowodu.findKategoriaPowodu(id);
        boolean modified =  dod.modifyKategoriaPowodu(obj);
        Integer currentVersion = obj.getVersion();
        KategoriaPowodu merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'KategoriaPowodu' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void KategoriaPowoduIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'KategoriaPowodu' failed to initialize correctly", dod.getRandomKategoriaPowodu());
        KategoriaPowodu obj = dod.getNewTransientKategoriaPowodu(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'KategoriaPowodu' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'KategoriaPowodu' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'KategoriaPowodu' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void KategoriaPowoduIntegrationTest.testRemove() {
        KategoriaPowodu obj = dod.getRandomKategoriaPowodu();
        Assert.assertNotNull("Data on demand for 'KategoriaPowodu' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'KategoriaPowodu' failed to provide an identifier", id);
        obj = KategoriaPowodu.findKategoriaPowodu(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'KategoriaPowodu' with identifier '" + id + "'", KategoriaPowodu.findKategoriaPowodu(id));
    }
    
}
