// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import pl.wroc.pwr.wiz.io.psi.model.Zdjecie;
import pl.wroc.pwr.wiz.io.psi.model.ZdjecieDataOnDemand;
import pl.wroc.pwr.wiz.io.psi.model.ZdjecieIntegrationTest;

privileged aspect ZdjecieIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ZdjecieIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ZdjecieIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ZdjecieIntegrationTest: @Transactional;
    
    @Autowired
    ZdjecieDataOnDemand ZdjecieIntegrationTest.dod;
    
    @Test
    public void ZdjecieIntegrationTest.testCountZdjecies() {
        Assert.assertNotNull("Data on demand for 'Zdjecie' failed to initialize correctly", dod.getRandomZdjecie());
        long count = Zdjecie.countZdjecies();
        Assert.assertTrue("Counter for 'Zdjecie' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ZdjecieIntegrationTest.testFindZdjecie() {
        Zdjecie obj = dod.getRandomZdjecie();
        Assert.assertNotNull("Data on demand for 'Zdjecie' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Zdjecie' failed to provide an identifier", id);
        obj = Zdjecie.findZdjecie(id);
        Assert.assertNotNull("Find method for 'Zdjecie' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Zdjecie' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ZdjecieIntegrationTest.testFindAllZdjecies() {
        Assert.assertNotNull("Data on demand for 'Zdjecie' failed to initialize correctly", dod.getRandomZdjecie());
        long count = Zdjecie.countZdjecies();
        Assert.assertTrue("Too expensive to perform a find all test for 'Zdjecie', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Zdjecie> result = Zdjecie.findAllZdjecies();
        Assert.assertNotNull("Find all method for 'Zdjecie' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Zdjecie' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ZdjecieIntegrationTest.testFindZdjecieEntries() {
        Assert.assertNotNull("Data on demand for 'Zdjecie' failed to initialize correctly", dod.getRandomZdjecie());
        long count = Zdjecie.countZdjecies();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Zdjecie> result = Zdjecie.findZdjecieEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Zdjecie' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Zdjecie' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ZdjecieIntegrationTest.testFlush() {
        Zdjecie obj = dod.getRandomZdjecie();
        Assert.assertNotNull("Data on demand for 'Zdjecie' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Zdjecie' failed to provide an identifier", id);
        obj = Zdjecie.findZdjecie(id);
        Assert.assertNotNull("Find method for 'Zdjecie' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyZdjecie(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Zdjecie' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ZdjecieIntegrationTest.testMergeUpdate() {
        Zdjecie obj = dod.getRandomZdjecie();
        Assert.assertNotNull("Data on demand for 'Zdjecie' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Zdjecie' failed to provide an identifier", id);
        obj = Zdjecie.findZdjecie(id);
        boolean modified =  dod.modifyZdjecie(obj);
        Integer currentVersion = obj.getVersion();
        Zdjecie merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'Zdjecie' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ZdjecieIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Zdjecie' failed to initialize correctly", dod.getRandomZdjecie());
        Zdjecie obj = dod.getNewTransientZdjecie(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Zdjecie' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Zdjecie' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'Zdjecie' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ZdjecieIntegrationTest.testRemove() {
        Zdjecie obj = dod.getRandomZdjecie();
        Assert.assertNotNull("Data on demand for 'Zdjecie' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Zdjecie' failed to provide an identifier", id);
        obj = Zdjecie.findZdjecie(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Zdjecie' with identifier '" + id + "'", Zdjecie.findZdjecie(id));
    }
    
}
