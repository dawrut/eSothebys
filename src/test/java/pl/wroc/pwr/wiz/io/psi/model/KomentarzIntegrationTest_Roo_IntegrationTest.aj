// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import pl.wroc.pwr.wiz.io.psi.model.Komentarz;
import pl.wroc.pwr.wiz.io.psi.model.KomentarzDataOnDemand;
import pl.wroc.pwr.wiz.io.psi.model.KomentarzIntegrationTest;

privileged aspect KomentarzIntegrationTest_Roo_IntegrationTest {
    
    declare @type: KomentarzIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: KomentarzIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: KomentarzIntegrationTest: @Transactional;
    
    @Autowired
    KomentarzDataOnDemand KomentarzIntegrationTest.dod;
    
    @Test
    public void KomentarzIntegrationTest.testCountKomentarzes() {
        Assert.assertNotNull("Data on demand for 'Komentarz' failed to initialize correctly", dod.getRandomKomentarz());
        long count = Komentarz.countKomentarzes();
        Assert.assertTrue("Counter for 'Komentarz' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void KomentarzIntegrationTest.testFindKomentarz() {
        Komentarz obj = dod.getRandomKomentarz();
        Assert.assertNotNull("Data on demand for 'Komentarz' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Komentarz' failed to provide an identifier", id);
        obj = Komentarz.findKomentarz(id);
        Assert.assertNotNull("Find method for 'Komentarz' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Komentarz' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void KomentarzIntegrationTest.testFindAllKomentarzes() {
        Assert.assertNotNull("Data on demand for 'Komentarz' failed to initialize correctly", dod.getRandomKomentarz());
        long count = Komentarz.countKomentarzes();
        Assert.assertTrue("Too expensive to perform a find all test for 'Komentarz', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Komentarz> result = Komentarz.findAllKomentarzes();
        Assert.assertNotNull("Find all method for 'Komentarz' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Komentarz' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void KomentarzIntegrationTest.testFindKomentarzEntries() {
        Assert.assertNotNull("Data on demand for 'Komentarz' failed to initialize correctly", dod.getRandomKomentarz());
        long count = Komentarz.countKomentarzes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Komentarz> result = Komentarz.findKomentarzEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Komentarz' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Komentarz' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void KomentarzIntegrationTest.testFlush() {
        Komentarz obj = dod.getRandomKomentarz();
        Assert.assertNotNull("Data on demand for 'Komentarz' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Komentarz' failed to provide an identifier", id);
        obj = Komentarz.findKomentarz(id);
        Assert.assertNotNull("Find method for 'Komentarz' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyKomentarz(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Komentarz' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void KomentarzIntegrationTest.testMergeUpdate() {
        Komentarz obj = dod.getRandomKomentarz();
        Assert.assertNotNull("Data on demand for 'Komentarz' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Komentarz' failed to provide an identifier", id);
        obj = Komentarz.findKomentarz(id);
        boolean modified =  dod.modifyKomentarz(obj);
        Integer currentVersion = obj.getVersion();
        Komentarz merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'Komentarz' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void KomentarzIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Komentarz' failed to initialize correctly", dod.getRandomKomentarz());
        Komentarz obj = dod.getNewTransientKomentarz(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Komentarz' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Komentarz' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'Komentarz' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void KomentarzIntegrationTest.testRemove() {
        Komentarz obj = dod.getRandomKomentarz();
        Assert.assertNotNull("Data on demand for 'Komentarz' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Komentarz' failed to provide an identifier", id);
        obj = Komentarz.findKomentarz(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Komentarz' with identifier '" + id + "'", Komentarz.findKomentarz(id));
    }
    
}
