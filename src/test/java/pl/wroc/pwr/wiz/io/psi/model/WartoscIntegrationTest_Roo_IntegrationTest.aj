// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import pl.wroc.pwr.wiz.io.psi.model.Wartosc;
import pl.wroc.pwr.wiz.io.psi.model.WartoscDataOnDemand;
import pl.wroc.pwr.wiz.io.psi.model.WartoscIntegrationTest;

privileged aspect WartoscIntegrationTest_Roo_IntegrationTest {
    
    declare @type: WartoscIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: WartoscIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: WartoscIntegrationTest: @Transactional;
    
    @Autowired
    WartoscDataOnDemand WartoscIntegrationTest.dod;
    
    @Test
    public void WartoscIntegrationTest.testCountWartoscs() {
        Assert.assertNotNull("Data on demand for 'Wartosc' failed to initialize correctly", dod.getRandomWartosc());
        long count = Wartosc.countWartoscs();
        Assert.assertTrue("Counter for 'Wartosc' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void WartoscIntegrationTest.testFindWartosc() {
        Wartosc obj = dod.getRandomWartosc();
        Assert.assertNotNull("Data on demand for 'Wartosc' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Wartosc' failed to provide an identifier", id);
        obj = Wartosc.findWartosc(id);
        Assert.assertNotNull("Find method for 'Wartosc' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Wartosc' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void WartoscIntegrationTest.testFindAllWartoscs() {
        Assert.assertNotNull("Data on demand for 'Wartosc' failed to initialize correctly", dod.getRandomWartosc());
        long count = Wartosc.countWartoscs();
        Assert.assertTrue("Too expensive to perform a find all test for 'Wartosc', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Wartosc> result = Wartosc.findAllWartoscs();
        Assert.assertNotNull("Find all method for 'Wartosc' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Wartosc' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void WartoscIntegrationTest.testFindWartoscEntries() {
        Assert.assertNotNull("Data on demand for 'Wartosc' failed to initialize correctly", dod.getRandomWartosc());
        long count = Wartosc.countWartoscs();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Wartosc> result = Wartosc.findWartoscEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Wartosc' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Wartosc' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void WartoscIntegrationTest.testFlush() {
        Wartosc obj = dod.getRandomWartosc();
        Assert.assertNotNull("Data on demand for 'Wartosc' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Wartosc' failed to provide an identifier", id);
        obj = Wartosc.findWartosc(id);
        Assert.assertNotNull("Find method for 'Wartosc' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyWartosc(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Wartosc' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void WartoscIntegrationTest.testMergeUpdate() {
        Wartosc obj = dod.getRandomWartosc();
        Assert.assertNotNull("Data on demand for 'Wartosc' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Wartosc' failed to provide an identifier", id);
        obj = Wartosc.findWartosc(id);
        boolean modified =  dod.modifyWartosc(obj);
        Integer currentVersion = obj.getVersion();
        Wartosc merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'Wartosc' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void WartoscIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Wartosc' failed to initialize correctly", dod.getRandomWartosc());
        Wartosc obj = dod.getNewTransientWartosc(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Wartosc' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Wartosc' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'Wartosc' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void WartoscIntegrationTest.testRemove() {
        Wartosc obj = dod.getRandomWartosc();
        Assert.assertNotNull("Data on demand for 'Wartosc' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Wartosc' failed to provide an identifier", id);
        obj = Wartosc.findWartosc(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Wartosc' with identifier '" + id + "'", Wartosc.findWartosc(id));
    }
    
}
