// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import pl.wroc.pwr.wiz.io.psi.model.Uczestnik;
import pl.wroc.pwr.wiz.io.psi.model.UczestnikDataOnDemand;
import pl.wroc.pwr.wiz.io.psi.model.UczestnikIntegrationTest;

privileged aspect UczestnikIntegrationTest_Roo_IntegrationTest {
    
    declare @type: UczestnikIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: UczestnikIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: UczestnikIntegrationTest: @Transactional;
    
    @Autowired
    UczestnikDataOnDemand UczestnikIntegrationTest.dod;
    
    @Test
    public void UczestnikIntegrationTest.testCountUczestniks() {
        Assert.assertNotNull("Data on demand for 'Uczestnik' failed to initialize correctly", dod.getRandomUczestnik());
        long count = Uczestnik.countUczestniks();
        Assert.assertTrue("Counter for 'Uczestnik' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void UczestnikIntegrationTest.testFindUczestnik() {
        Uczestnik obj = dod.getRandomUczestnik();
        Assert.assertNotNull("Data on demand for 'Uczestnik' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Uczestnik' failed to provide an identifier", id);
        obj = Uczestnik.findUczestnik(id);
        Assert.assertNotNull("Find method for 'Uczestnik' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Uczestnik' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void UczestnikIntegrationTest.testFindAllUczestniks() {
        Assert.assertNotNull("Data on demand for 'Uczestnik' failed to initialize correctly", dod.getRandomUczestnik());
        long count = Uczestnik.countUczestniks();
        Assert.assertTrue("Too expensive to perform a find all test for 'Uczestnik', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Uczestnik> result = Uczestnik.findAllUczestniks();
        Assert.assertNotNull("Find all method for 'Uczestnik' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Uczestnik' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void UczestnikIntegrationTest.testFindUczestnikEntries() {
        Assert.assertNotNull("Data on demand for 'Uczestnik' failed to initialize correctly", dod.getRandomUczestnik());
        long count = Uczestnik.countUczestniks();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Uczestnik> result = Uczestnik.findUczestnikEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Uczestnik' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Uczestnik' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void UczestnikIntegrationTest.testFlush() {
        Uczestnik obj = dod.getRandomUczestnik();
        Assert.assertNotNull("Data on demand for 'Uczestnik' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Uczestnik' failed to provide an identifier", id);
        obj = Uczestnik.findUczestnik(id);
        Assert.assertNotNull("Find method for 'Uczestnik' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyUczestnik(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Uczestnik' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void UczestnikIntegrationTest.testMergeUpdate() {
        Uczestnik obj = dod.getRandomUczestnik();
        Assert.assertNotNull("Data on demand for 'Uczestnik' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Uczestnik' failed to provide an identifier", id);
        obj = Uczestnik.findUczestnik(id);
        boolean modified =  dod.modifyUczestnik(obj);
        Integer currentVersion = obj.getVersion();
        Uczestnik merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'Uczestnik' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void UczestnikIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Uczestnik' failed to initialize correctly", dod.getRandomUczestnik());
        Uczestnik obj = dod.getNewTransientUczestnik(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Uczestnik' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Uczestnik' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'Uczestnik' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void UczestnikIntegrationTest.testRemove() {
        Uczestnik obj = dod.getRandomUczestnik();
        Assert.assertNotNull("Data on demand for 'Uczestnik' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Uczestnik' failed to provide an identifier", id);
        obj = Uczestnik.findUczestnik(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Uczestnik' with identifier '" + id + "'", Uczestnik.findUczestnik(id));
    }
    
}
