// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;
import pl.wroc.pwr.wiz.io.psi.model.KontoFakturowe;
import pl.wroc.pwr.wiz.io.psi.model.KontoFakturoweDataOnDemand;
import pl.wroc.pwr.wiz.io.psi.model.Uzytkownik;

privileged aspect KontoFakturoweDataOnDemand_Roo_DataOnDemand {
    
    declare @type: KontoFakturoweDataOnDemand: @Component;
    
    private Random KontoFakturoweDataOnDemand.rnd = new SecureRandom();
    
    private List<KontoFakturowe> KontoFakturoweDataOnDemand.data;
    
    public KontoFakturowe KontoFakturoweDataOnDemand.getNewTransientKontoFakturowe(int index) {
        KontoFakturowe obj = new KontoFakturowe();
        setNazwaDoFaktury(obj, index);
        setNip(obj, index);
        setUzytkownik(obj, index);
        return obj;
    }
    
    public void KontoFakturoweDataOnDemand.setNazwaDoFaktury(KontoFakturowe obj, int index) {
        String nazwaDoFaktury = "nazwaDoFaktury_" + index;
        obj.setNazwaDoFaktury(nazwaDoFaktury);
    }
    
    public void KontoFakturoweDataOnDemand.setNip(KontoFakturowe obj, int index) {
        String nip = "nip_" + index;
        obj.setNip(nip);
    }
    
    public void KontoFakturoweDataOnDemand.setUzytkownik(KontoFakturowe obj, int index) {
        Uzytkownik uzytkownik = null;
        obj.setUzytkownik(uzytkownik);
    }
    
    public KontoFakturowe KontoFakturoweDataOnDemand.getSpecificKontoFakturowe(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        KontoFakturowe obj = data.get(index);
        Long id = obj.getId();
        return KontoFakturowe.findKontoFakturowe(id);
    }
    
    public KontoFakturowe KontoFakturoweDataOnDemand.getRandomKontoFakturowe() {
        init();
        KontoFakturowe obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return KontoFakturowe.findKontoFakturowe(id);
    }
    
    public boolean KontoFakturoweDataOnDemand.modifyKontoFakturowe(KontoFakturowe obj) {
        return false;
    }
    
    public void KontoFakturoweDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = KontoFakturowe.findKontoFakturoweEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'KontoFakturowe' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<KontoFakturowe>();
        for (int i = 0; i < 10; i++) {
            KontoFakturowe obj = getNewTransientKontoFakturowe(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
