// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;
import pl.wroc.pwr.wiz.io.psi.model.Kraj;
import pl.wroc.pwr.wiz.io.psi.model.KrajDataOnDemand;
import pl.wroc.pwr.wiz.io.psi.service.dao.KrajService;

privileged aspect KrajDataOnDemand_Roo_DataOnDemand {
    
    declare @type: KrajDataOnDemand: @Component;
    
    private Random KrajDataOnDemand.rnd = new SecureRandom();
    
    private List<Kraj> KrajDataOnDemand.data;
    
    @Autowired
    KrajService KrajDataOnDemand.krajService;
    
    public Kraj KrajDataOnDemand.getNewTransientKraj(int index) {
        Kraj obj = new Kraj();
        setNazwa(obj, index);
        return obj;
    }
    
    public void KrajDataOnDemand.setNazwa(Kraj obj, int index) {
        String nazwa = "nazwa_" + index;
        obj.setNazwa(nazwa);
    }
    
    public Kraj KrajDataOnDemand.getSpecificKraj(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Kraj obj = data.get(index);
        Long id = obj.getId();
        return krajService.findKraj(id);
    }
    
    public Kraj KrajDataOnDemand.getRandomKraj() {
        init();
        Kraj obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return krajService.findKraj(id);
    }
    
    public boolean KrajDataOnDemand.modifyKraj(Kraj obj) {
        return false;
    }
    
    public void KrajDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = krajService.findKrajEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Kraj' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Kraj>();
        for (int i = 0; i < 10; i++) {
            Kraj obj = getNewTransientKraj(i);
            try {
                krajService.saveKraj(obj);
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
