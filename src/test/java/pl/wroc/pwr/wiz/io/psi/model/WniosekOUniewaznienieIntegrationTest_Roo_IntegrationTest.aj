// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package pl.wroc.pwr.wiz.io.psi.model;

import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;
import pl.wroc.pwr.wiz.io.psi.model.WniosekOUniewaznienie;
import pl.wroc.pwr.wiz.io.psi.model.WniosekOUniewaznienieDataOnDemand;
import pl.wroc.pwr.wiz.io.psi.model.WniosekOUniewaznienieIntegrationTest;

privileged aspect WniosekOUniewaznienieIntegrationTest_Roo_IntegrationTest {
    
    declare @type: WniosekOUniewaznienieIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: WniosekOUniewaznienieIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: WniosekOUniewaznienieIntegrationTest: @Transactional;
    
    @Autowired
    WniosekOUniewaznienieDataOnDemand WniosekOUniewaznienieIntegrationTest.dod;
    
    @Test
    public void WniosekOUniewaznienieIntegrationTest.testCountWniosekOUniewaznienies() {
        Assert.assertNotNull("Data on demand for 'WniosekOUniewaznienie' failed to initialize correctly", dod.getRandomWniosekOUniewaznienie());
        long count = WniosekOUniewaznienie.countWniosekOUniewaznienies();
        Assert.assertTrue("Counter for 'WniosekOUniewaznienie' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void WniosekOUniewaznienieIntegrationTest.testFindWniosekOUniewaznienie() {
        WniosekOUniewaznienie obj = dod.getRandomWniosekOUniewaznienie();
        Assert.assertNotNull("Data on demand for 'WniosekOUniewaznienie' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'WniosekOUniewaznienie' failed to provide an identifier", id);
        obj = WniosekOUniewaznienie.findWniosekOUniewaznienie(id);
        Assert.assertNotNull("Find method for 'WniosekOUniewaznienie' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'WniosekOUniewaznienie' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void WniosekOUniewaznienieIntegrationTest.testFindAllWniosekOUniewaznienies() {
        Assert.assertNotNull("Data on demand for 'WniosekOUniewaznienie' failed to initialize correctly", dod.getRandomWniosekOUniewaznienie());
        long count = WniosekOUniewaznienie.countWniosekOUniewaznienies();
        Assert.assertTrue("Too expensive to perform a find all test for 'WniosekOUniewaznienie', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<WniosekOUniewaznienie> result = WniosekOUniewaznienie.findAllWniosekOUniewaznienies();
        Assert.assertNotNull("Find all method for 'WniosekOUniewaznienie' illegally returned null", result);
        Assert.assertTrue("Find all method for 'WniosekOUniewaznienie' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void WniosekOUniewaznienieIntegrationTest.testFindWniosekOUniewaznienieEntries() {
        Assert.assertNotNull("Data on demand for 'WniosekOUniewaznienie' failed to initialize correctly", dod.getRandomWniosekOUniewaznienie());
        long count = WniosekOUniewaznienie.countWniosekOUniewaznienies();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<WniosekOUniewaznienie> result = WniosekOUniewaznienie.findWniosekOUniewaznienieEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'WniosekOUniewaznienie' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'WniosekOUniewaznienie' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void WniosekOUniewaznienieIntegrationTest.testFlush() {
        WniosekOUniewaznienie obj = dod.getRandomWniosekOUniewaznienie();
        Assert.assertNotNull("Data on demand for 'WniosekOUniewaznienie' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'WniosekOUniewaznienie' failed to provide an identifier", id);
        obj = WniosekOUniewaznienie.findWniosekOUniewaznienie(id);
        Assert.assertNotNull("Find method for 'WniosekOUniewaznienie' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyWniosekOUniewaznienie(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'WniosekOUniewaznienie' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void WniosekOUniewaznienieIntegrationTest.testMergeUpdate() {
        WniosekOUniewaznienie obj = dod.getRandomWniosekOUniewaznienie();
        Assert.assertNotNull("Data on demand for 'WniosekOUniewaznienie' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'WniosekOUniewaznienie' failed to provide an identifier", id);
        obj = WniosekOUniewaznienie.findWniosekOUniewaznienie(id);
        boolean modified =  dod.modifyWniosekOUniewaznienie(obj);
        Integer currentVersion = obj.getVersion();
        WniosekOUniewaznienie merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'WniosekOUniewaznienie' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void WniosekOUniewaznienieIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'WniosekOUniewaznienie' failed to initialize correctly", dod.getRandomWniosekOUniewaznienie());
        WniosekOUniewaznienie obj = dod.getNewTransientWniosekOUniewaznienie(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'WniosekOUniewaznienie' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'WniosekOUniewaznienie' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'WniosekOUniewaznienie' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void WniosekOUniewaznienieIntegrationTest.testRemove() {
        WniosekOUniewaznienie obj = dod.getRandomWniosekOUniewaznienie();
        Assert.assertNotNull("Data on demand for 'WniosekOUniewaznienie' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'WniosekOUniewaznienie' failed to provide an identifier", id);
        obj = WniosekOUniewaznienie.findWniosekOUniewaznienie(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'WniosekOUniewaznienie' with identifier '" + id + "'", WniosekOUniewaznienie.findWniosekOUniewaznienie(id));
    }
    
}
